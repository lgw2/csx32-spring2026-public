%!TEX TS-program = pdflatex
%!TEX encoding = UTF-8 Unicode
%!TEX spellcheck = English (Aspell)

\documentclass[11pt]{article}
\usepackage{jeffe,handout,graphicx}

\def\Cdot{\mathbin{\text{\normalfont \textbullet}}}
\def\Sym#1{\textbf{\texttt{\color{BrickRed}#1}}}

\newcommand{\X}[1]{\textcolor{Red}{\mathbf{\underline{#1}}}}

\hidesolutions

% =========================================================
\begin{document}

\headers{CSCI 432/532}{Problem Session 10}{Spring 2026}

\noindent

\begin{enumerate}\itemsep3ex\parindent1.5em

    \item

Choose two polynomials $A(x)$ and $B(x)$of degree three (i.e., largest term is cubic) and trace through the FFT algorithm
to compute the product of the two polynomials, $C(x)$.
\end{enumerate}

\end{document}